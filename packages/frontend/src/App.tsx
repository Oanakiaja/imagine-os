/**
 * App Component - V1.0
 *
 * 主应用入口
 *
 * 版本说明：
 * - V1.0: 使用 Desktop with all new features
 * - 包含错误边界（未来添加）
 */

import { Desktop } from './components/Desktop';

// 未来可以添加 ErrorBoundary
function App() {
  return <Desktop />;
}

export default App;
